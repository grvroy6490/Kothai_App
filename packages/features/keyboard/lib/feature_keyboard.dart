library feature_keyboard;

import 'package:flutter/material.dart';

class KeyboardDebugView extends StatelessWidget {
  const KeyboardDebugView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Keyboard feature placeholder'));
  }
}
